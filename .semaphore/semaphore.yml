version: "v1.0"
name: First pipeline example
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804

blocks:
  - name: "Test & Build"
    task:
      env_vars:
        - name: APP_ENV
          value: prod
        - name: GOPATH
          value: /home/semaphore/go
      jobs:
      - name: Unit Tests
        commands:
          - checkout
          - echo "Building usable go workspace"
          - mkdir -p /home/semaphore/go/src/github.com/Fresh-Tracks
          - ln -s /home/semaphore/bomb-squad/vendor ${GOPATH}/src
          - ln -s /home/semaphore/bomb-squad ${GOPATH}/src/github.com/Fresh-Tracks/bomb-squad
          - echo ${GOPATH} && ls -1 ${GOPATH}/src
          - ls -1 ${GOPATH}/src/Fresh-Tracks
          - echo "Running unit tests..."
          - go test -v ./...
      - name: Docker build
        commands:
          - checkout
          - ls -1
          - pwd
          - echo "Building image..."
          - make
