version: "v1.0"
name: First pipeline example
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804

blocks:
  - name: "Build"
    task:
      env_vars:
        - name: GOPATH
          value: /home/semaphore/bomb-squad/vendor
      jobs:
      - name: Docker build
        commands:
          - checkout
          - ls -1
          - pwd
          - echo "Building image"
          - make
